name: Preview Android build

on:
  workflow_dispatch:
    inputs:
      profile:
        type: choice
        options:
          - preview
          - development
        default: preview

jobs:
  build_android:
    name: Build Android
    type: build
    params:
      platform: android
      profile: ${{ inputs.profile || 'preview' }}
