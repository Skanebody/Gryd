/**
 * GRYD — onglet Carte (home) : la carte EST le produit (SPEC §4.2.1).
 * Le bouton COURIR et la nav vivent dans le layout (tabs) — pas de doublon ici.
 */
export { default } from '../../src/features/map/MapScreen';
