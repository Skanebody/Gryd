/**
 * GRYD — `/notifications` : le centre d'activité de §14.2.
 *
 * La porte principale est la CLOCHE de l'en-tête de la carte (`MapHome`) ; le
 * Profil en ouvre une seconde. `/activite`, la route legacy, redirige ici.
 */
export { default } from '../src/features/notifications/NotificationCenter2026';
