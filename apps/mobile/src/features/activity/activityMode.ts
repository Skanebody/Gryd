/**
 * GRYD — re-export compat. La source de vérité est `playContext.ts`
 * (activité Run/Bike + social Solo/Crew).
 */
export {
  useActivityMode,
  usePlayContext,
  type ActivityMode,
  type SocialScope,
  type PlayContext,
} from './playContext';
