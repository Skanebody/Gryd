/** Shared geometry for the three destinations and the map's integrated action. */
export const NAV_BAR_HEIGHT = 54;
export const NAV_BOTTOM_GAP = 22;
export const NAV_MAP_BAR_HEIGHT = 60;
export const NAV_MAP_BOTTOM_GAP = 16;
export const NAV_MAP_MAX_WIDTH = 360;
/** Transitional spacing retained for the recording banner, no longer a floating GO button. */
export const GO_BUTTON_GAP = 18;
/** Scroll content clears the whole floating dock, including its breathing room. */
export const TAB_CONTENT_BOTTOM_CLEARANCE = NAV_BAR_HEIGHT + NAV_BOTTOM_GAP + 20;
