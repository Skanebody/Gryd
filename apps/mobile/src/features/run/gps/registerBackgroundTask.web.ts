/**
 * GRYD — variante WEB de registerBackgroundTask : volontairement vide.
 * Le GPS réel n'existe que sur appareil natif (AMENDEMENT-15 §0) — le preview
 * web garde la simulation démo, aucun module natif dans le bundle.
 */
export {};
